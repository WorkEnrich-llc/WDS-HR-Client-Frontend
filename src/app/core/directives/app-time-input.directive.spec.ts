import { AppTimeInputDirective } from './app-time-input.directive';

describe('AppTimeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTimeInputDirective();
    expect(directive).toBeTruthy();
  });
});
